pipeline {
    agent any
    stages {
        stage('CNN') {
            steps {
                sh 'pwd'
                sh 'git --version'
                sh 'docker ps'
            }
        }      
    }
}
